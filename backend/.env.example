# Copy this file to .env and fill in real values, OR export DATABASE_URL in your shell.
# PostgreSQL:
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/library_db

# To use SQLite instead (no Postgres needed), uncomment:
# DATABASE_URL=sqlite:///./library.db
